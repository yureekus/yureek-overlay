name: Update Equibop, arRPC, FlClashX, and Ryubing Canary

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0,12 * * *"

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.OVERLAY_PUSH_TOKEN || github.token }}

      - name: Update ebuild and manifest
        run: |
          .github/scripts/bump-equibop-flclashx.sh

      - name: Commit and push directly to main
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add app-misc/arrpc-bun games-emulation/ryubing-canary net-im/equibop net-proxy/flclashx
          git commit -m "overlay: bump Equibop, arRPC, FlClashX, and Ryubing Canary to latest upstream releases"
          git pull --rebase origin main
          git push origin HEAD:main
